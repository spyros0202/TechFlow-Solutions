version: "3.9"

services:
  web:
    build: .
    container_name: automation_project_web
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8001"
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      DJANGO_SETTINGS_MODULE: "automation_project.settings"
    volumes:
      - .:/app
    restart: unless-stopped
